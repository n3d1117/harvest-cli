interface:
  display_name: "Harvest"
  short_description: "Use the Harvest time-entry and submit CLI"
  default_prompt: "Use $harvest to inspect Harvest config, create or change time entries, or submit a week for approval with the CLI."
