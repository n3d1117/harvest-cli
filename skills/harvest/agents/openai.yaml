interface:
  display_name: "Harvest"
  short_description: "Use the Harvest time logging and submit CLI"
  default_prompt: "Use $harvest to inspect Harvest config, log time, or submit a week for approval with the CLI."
